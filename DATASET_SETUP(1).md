# Dataset Setup

The project expects a HotpotQA-compatible development file at:

```text
data/dev.jsonl
```

Each record should contain at least `_id`, `question`, and `answer`.

Do not commit the full dataset unless its license and repository size permit it. Document the official source and any preprocessing steps instead.
